name: CI — Targeting Service

on:
  workflow_dispatch:
  push:
    paths:
      - 'services/targeting-service-main/**'
      - 'k8s/**'
      - 'docker-compose.yml'
      - '.github/workflows/**'
  pull_request:
    paths:
      - 'services/targeting-service-main/**'
      - 'k8s/**'
      - 'docker-compose.yml'
      - '.github/workflows/**'

jobs:
  call:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      service: 'targeting'
      context: 'services/targeting-service-main'
      dockerfile: 'Dockerfile'
      image_name: 'targeting-service'
      language: 'python'
      push: ${{ github.ref == 'refs/heads/main' }}
      tests_command: 'python -m pytest test_app.py -v --tb=short'
      update_k8s_manifest: ${{ true }}
      k8s_manifest_path: 'k8s/targeting-service/deployment.yaml'
      k8s_image_prefix: '576394965690.dkr.ecr.us-east-1.amazonaws.com/targeting-service'
      k8s_container_name: 'targeting-service'
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: ${{ secrets.AWS_REGION }}
      K8S_REPO_TOKEN: ${{ secrets.K8S_REPO_TOKEN }}
