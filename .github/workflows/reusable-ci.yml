name: Reusable CI

on:
  workflow_call:
    secrets:
      ecr_repo_prefix: #tech-challenge
        required: true
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      AWS_SESSION_TOKEN: #only used in aws learner lab
        required: false
      AWS_REGION: #us-east-1
        required: true
    inputs:
      service:
        required: true
        type: string
      context:
        required: false
        type: string
        default: ''
      dockerfile:
        required: false
        type: string
        default: 'Dockerfile'
      image_name:
        required: false
        type: string
        default: ''
      language:
        required: false
        type: string
        default: 'go'
      push:
        required: false
        type: boolean
        default: false
      ecr_registry:
        required: false
        type: string
        default: ''
      tests_command:
        required: false
        type: string
        default: ''

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.set-image.outputs.image }}
      context: ${{ steps.ctx.outputs.context }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Determine context
        id: ctx
        run: |
          if [ -z "${{ inputs.context }}" ]; then
            echo "context=services/${{ inputs.service }}-main" >> $GITHUB_OUTPUT
          else
            echo "context=${{ inputs.context }}" >> $GITHUB_OUTPUT
          fi

      - name: Set image name and tag
        id: set-image
        run: |
          SHORT=${GITHUB_SHA::7}
          TAG=sha-${SHORT}
          IMAGE_NAME="${{ inputs.image_name }}"
          if [ -z "$IMAGE_NAME" ]; then
            IMAGE_NAME="${{ inputs.service }}-service"
          fi
          echo "image=${IMAGE_NAME}:${TAG}" >> $GITHUB_OUTPUT

  deps_build_and_scan:
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        if: ${{ inputs.language == 'go' }}
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Cache Go modules
        if: ${{ inputs.language == 'go' }}
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ${{ github.workspace }}/.cache/gomod
          key: go-mod-${{ runner.os }}-${{ hashFiles('**/go.sum') }}

      - name: Setup Python
        if: ${{ inputs.language == 'python' }}
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Cache pip
        if: ${{ inputs.language == 'python' }}
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}

      - name: Install dev tools (Go)
        if: ${{ inputs.language == 'go' }}
        working-directory: ${{ needs.prepare.outputs.context }}
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          go vet ./...

      - name: Install dev tools (Python)
        if: ${{ inputs.language == 'python' }}
        run: |
          python -m pip install --upgrade pip
          python -m pip install bandit pip-audit pytest

      - name: Run dependency security scan (Go)
        if: ${{ inputs.language == 'go' }}
        working-directory: ${{ needs.prepare.outputs.context }}
        run: gosec ./...

      - name: Run dependency security scan (Python)
        if: ${{ inputs.language == 'python' }}
        working-directory: ${{ needs.prepare.outputs.context }}
        run: pip-audit -r requirements.txt || true

      - name: Run tests (Go)
        if: ${{ inputs.language == 'go' && inputs.tests_command != '' }}
        working-directory: ${{ needs.prepare.outputs.context }}
        run: ${{ inputs.tests_command }}

      - name: Run tests (Python)
        if: ${{ inputs.language == 'python' && inputs.tests_command != '' }}
        working-directory: ${{ needs.prepare.outputs.context }}
        env:
          ENVIRONMENT: test
        run: |
          pip install -r requirements.txt
          ${{ inputs.tests_command }}

  docker_build:
    needs:
      - prepare
      - deps_build_and_scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Build Docker image
        env:
          CTX: ${{ needs.prepare.outputs.context }}
          IMG: ${{ needs.prepare.outputs.image }}
        run: |
          docker build -f "$CTX/${{ inputs.dockerfile }}" -t "$IMG" "$CTX"
          if [ "$GITHUB_REF" = "refs/heads/main" ]; then
            docker tag "$IMG" "${IMG%:*}:latest" || true
          fi

      - name: Container scan (Trivy)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ needs.prepare.outputs.image }}
          scan-type: image
          severity: CRITICAL,HIGH

      - name: Save image for downstream job
        if: ${{ inputs.push }}
        env:
          IMG: ${{ needs.prepare.outputs.image }}
        run: docker save "$IMG" -o /tmp/image.tar

      - name: Upload image artifact
        if: ${{ inputs.push }}
        uses: actions/upload-artifact@v4
        with:
          name: docker-image-${{ inputs.service }}
          path: /tmp/image.tar
          retention-days: 1

  push_ecr:
    needs:
      - prepare
      - docker_build
    if: ${{ inputs.push }}
    runs-on: ubuntu-latest
    steps:
      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image-${{ inputs.service }}
          path: /tmp

      - name: Load image
        run: docker load -i /tmp/image.tar

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Push to ECR
        env:
          IMAGE: ${{ needs.prepare.outputs.image }}
          ECR_REG: ${{ steps.ecr-login.outputs.registry }}
          ECR_PREFIX: ${{ secrets.ecr_repo_prefix }}
        run: |
          SHORT=${GITHUB_SHA::7}
          TAG=sha-${SHORT}
          REPO_NAME="${ECR_PREFIX}/${{ inputs.image_name }}"
          FULL="${ECR_REG}/${REPO_NAME}"
          docker tag "$IMAGE" "${FULL}:${TAG}"
          docker push "${FULL}:${TAG}"
          if [ "$GITHUB_REF" = "refs/heads/main" ]; then
            docker tag "$IMAGE" "${FULL}:latest" || true
            docker push "${FULL}:latest"
          fi